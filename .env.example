# Supabase config for the /problems page.
#
# These are the same values used by the original Flutter app -- a public
# "anon"/publishable key gated by Row Level Security (read-only access to
# the solved_problems table), safe to ship in client code. Copy this file
# to `.env.local` (gitignored) and the site works immediately with no
# further setup.
#
#   cp .env.example .env.local
#
VITE_SUPABASE_URL=https://jklbxzejmuemezcnqwts.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_imN4gEGhFOEUWq7r8VRpCQ_OfllaoTO
